# rlm-mcp
Harness-agnostic MCP server that brings the Recursive Language Model (RLM) paradigm to any agent: context as a variable in a persistent REPL, symbolic recursion driven by the harness's own model. No API keys.
